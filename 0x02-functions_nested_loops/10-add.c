#include "main.h"

/**
 * add - adds two integers and returns the the result
 * @a: int type number
 * @b: int type number
 * Return: result of addition
 */
int add(int a, int b)
{
	return (a + b);
}
